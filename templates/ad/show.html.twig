{% extends 'base.html.twig' %}

{% block title %}
{{ad.title}}
{% endblock %}

{% block body %}
<div class="ad-title" style="background-image:url({{ad.coverImage}})">
<div class="container">
    <h1>{{ad.title}}</h1>
    <p>{{ad.introduction}}</p>
     <p class="h2">
     <strong>{{ad.rooms}}chambres </strong> pour <strong>{{ad.price}}&euro; </strong> par nuit
     </p>
     <a class="btn btn-primary btn-lg" href="#">Reserver</a>                
    </div>
</div>

<div class="container">
    <div class="row">
                <div class="col-md-8 mb-3">
                    {{ad.content | raw}}





<div id="carouselExampleIndicators" class="carousel slide mb-5" data-ride="carousel">
        <ol class="carousel-indicators">
        {% for image in ad.images %}
            <li data-target="#carouselExampleIndicators" data-slide-to="{{loop.index0}}" {% if loop.first %} class="active" {% endif %} ></li>
            {% endfor %}
        </ol>
        <div class="carousel-inner">
        {% for image in ad.images %}
            <div class="carousel-item {% if loop.first %} active {% endif %}">
            <img src="{{image.url}}" class="d-block w-100" alt="...">
                    <div class="carousel-caption d-none d-md-block">
                <h5>{{ad.title}}</h5>
                <p>{{image.caption}}</p>
            </div>
            </div>
            {% endfor %}
        </div>
        <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
</div>

    
                </div>
                
                <div class="col">
                         <div class="row mb-3 align-items">
                                 <div class="col-md">
                                    <img src="https://placehold.it/64*64" alt="essai" style="">
                                <div>

                                <div class="col-md"> 
                                    <h3>Joseph Dupond</h3>
                                    <spam class="badge badge-primary bg-primary">3 Annonces</spam>
                                <div> 
                        <div> 
                        <p class="mt-4">Placeat impedit quo pariatur incidunt inventore consequatur.
                        Reprehenderit dolorem illo delectus deserunt dolores voluptas.
                        Repudiandae molestiae eum dolor ipsam ut placeat accusamus.</p>
                        <p>Placeat impedit quo pariatur incidunt inventore consequatur.
                        Reprehenderit dolorem illo delectus deserunt dolores voluptas.
                        Repudiandae molestiae eum dolor ipsam ut placeat accusamus.</p>
                        
                </div>
    <div>
<div> 
    {% endblock %}