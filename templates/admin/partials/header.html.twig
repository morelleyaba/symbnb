
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('homepage') }}">SymBNB !</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="#">
          <i class="fa fa-line-chart"></i>Dashboard
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('admin_ads_index') }}">
          <i class="fa fa-table"></i>Annonces
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{path('admin_bookings_index')}}">
          <i class="fa fa-folder"></i>Reservations</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{path('admin_comments_index')}}">
          <i class="fa fa-comments"></i>Commentaire
          </a>
        <li class="nav-item">
          <a class="nav-link active" href="#">
          <i class="fa fa-users"></i>utilisateurs
          </a>
        </li>
        </li>
      </ul>
     <ul class="navbar-nav ml-auto">
     {# Si l'utilisateur est connecté ["on le saura grace a la variable global "app" [app.user] "] alors lui permetre de se deconnecter #}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('homepage') }}" target="_bank">
            <i class="fa fa-eye"></i>Voir le site
            </a>
          </li>
     {% if app.user %}
          {# creeons notre dropdown #}
          <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                {# Ajouter sa photo dans le navBar #}
                <img src="{{app.user.picture}}" class="avatar avatar-micro" alt="Avatar de {{app.user.fullName}}">
                {{app.user.fullName}}
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                {# l'admin se deconnecte via sa nouvelle route qui a été creé #}
                    <a class="dropdown-item" href="{{ path('admin_account_logout') }}">Deconnexion</a>
                </div>
          </li>
     {% endif %}
      </ul>
    </div>
  </div>
</nav>

{# message flash #}
{% for label, messages in app.flashes %}

<div class="container">
    <div class="alert alert-{{ label }}">
        {% for message in messages %}
          <p>{{ message|raw }}</p>
        {% endfor %}
    </div>
</div>
{% endfor %}