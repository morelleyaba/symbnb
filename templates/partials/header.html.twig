
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('homepage') }}">SymBNB !</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('homepage') }}">
          Acceuil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('ads_index') }}">
          Annonces</a>
        </li>
      </ul>
     <ul class="navbar-nav ml-auto">
     {# Si l'utilisateur est connecté ["on le saura grace a la variable global "app" [app.user] "] alors lui permetre de se deconnecter #}
     {% if app.user %}
          {# <li class="nav-item">
            <a class="nav-link active" href="{{ path('app_logout') }}">Deconnexion</a>
          </li> #}

          {# creeons notre dropdown #}
          <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                {# Ajouter sa photo dans le navBar #}
                <img src="{{app.user.picture}}" style="width:35px;height:35px; border-radius: 50%" alt="Avatar de {{app.user.fullName}}">
                {{app.user.fullName}}
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                    <a class="dropdown-item" href="{{ path('account_index') }}">Mon Compte</a>
                    <a class="dropdown-item" href="{{ path('account_profile') }}">Modifier mon profil</a>
                    <a class="dropdown-item" href="{{ path('account_bookings') }}">Mes reservations</a>
                    <a class="dropdown-item" href="{{ path('ads_index') }}">Acceuil</a>
                    {# Ajouter un trait de separation #}
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Deconnexion</a>
                </div>
          </li>
        {# sinon lui permettre de s'inscrire et/ou se connecter #}
     {% else %}
          <li class="nav-item">
            <a class="nav-link active" href="{{ path('account_register') }}">Inscription</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="{{ path('app_login') }}">Connexion</a>
          </li>
       
     {% endif %}
      </ul>
    </div>
  </div>
</nav>

{# message flash #}
{% for label, messages in app.flashes %}

<div class="container">
    <div class="alert alert-{{ label }}">
        {% for message in messages %}
          <p>{{ message|raw }}</p>
        {% endfor %}
    </div>
</div>
{% endfor %}