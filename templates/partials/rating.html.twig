
{# issu du fichier ad/show.html.twig #}
 {# le nbre des etoiles : {{comment.rating}} #}
 {# a chaque fois que je vais appeler le template, faudrais au moins que j'ai la variable "raiting", donc remplacons "comment.rating" par "rating" #}
                            
        {# les etoiles, si pour "i" allant de 1 de 5 , i <= au nbre de "rating" alors afficher les etoiles pleines sinon afficher les etoiles vides  #}
        <span class="rating">
            {% for i in 1..5 %}
                {# {% if i <= comment.rating %} #}
                {% if i <= rating %}
                <i class="fa fa-star"></i>
                {% else %}
                <i class="fa fa-star-o"></i>
                {% endif %}
            {% endfor %}
        </span>